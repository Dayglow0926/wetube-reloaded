extends base.pug

block content 
    h4 Change Title of video
    form(method="POST") 
        input(name="title", type="text", placeholder="Video Title" value=video.title)
        input(type="submit", value="Save")